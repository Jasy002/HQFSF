classifier:

  default: random_forest

  random_forest:
    n_estimators: 100
    max_depth: null
    random_state: 42

  svm:
    kernel: rbf
    C: 1.0
    gamma: scale

  logistic_regression:
    max_iter: 1000
    solver: lbfgs

  decision_tree:
    criterion: gini
    max_depth: null